export interface ClientModelForm{
    id?:number //siginifica que tanto faz na hora de cadastrar
    name: string
    email: string
    phone: string
}

export interface ClientModelTable{
    id:number
    name: string
    email: string
    phone: string
}